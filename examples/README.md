# Instructions
1. Run `pip3 install -r requirements.txt`. Vectorlite's metadata(rowid) filter feature requires sqlite3 version >= 3.38. Python 3.14's built-in `sqlite3` module bundles SQLite 3.50.4 (>= 3.38), so no extra driver is needed.